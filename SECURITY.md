# 安全策略

NucleiAI 是一个**授权安全测试**工具，本身也可能存在安全问题。我们感谢负责任的安全研究者。

## 支持的版本

当前只维护 `master` 分支的最新版本。

## 报告漏洞

**不要公开提 Issue。** 请通过以下方式之一报告：

- GitHub Security Advisory（仓库 → Security → Report a vulnerability）
- 邮件：在 GitHub 仓库页面查看作者邮箱

请提供：

1. 漏洞类型与影响
2. 复现步骤（尽量最小化）
3. 受影响的版本/代码位置

## 已知风险与缓解

| 风险 | 说明 | 缓解 |
|---|---|---|
| 未授权使用扫描能力 | Web 面板可驱动本机 nuclei 扫描任意目标（SSRF 类风险） | 默认仅绑定 127.0.0.1；设置 NUCLEIAI_AUTH_TOKEN；仅对可信目标使用 |
| LLM 误判 | 8B 模型存在波动，可能误报/漏报 | 三分类 + 硬规则；needs_review 结果必须人工复核 |
| 敏感数据 | 扫描结果可能包含敏感信息 | 优先本地 Ollama；使用云 API 时确认服务商数据处理条款 |

## 使用底线

仅扫描你拥有权限的目标。未授权扫描可能违反当地法律，本项目作者不对任何滥用行为负责。
